$> ls -l | grep libft; echo "hello    my friend      !";ls
$> echo "hello     my friend   !"; ls -l | grep lib; whoami  (check whitespaces after dquotes in last echo arg)
$> ls > sortie.txt | echo yes | grep s
$> ls -l | grep libft | wc -l < Makefile (check stdin of the last command)
$> ls -l  |grep libft |Rev (check return value 127)